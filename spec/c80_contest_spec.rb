require "spec_helper"

RSpec.describe C80Contest do
  it "has a version number" do
    expect(C80Contest::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
